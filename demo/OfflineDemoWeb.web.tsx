import React, { useEffect, useState } from 'react';
import { acceptQuote, applyRestaurant, cancelRequest, createDemoOtp, decideApplication, DemoOtp, DemoState, Food, money, mutate, Order, placeOrder, publishRestaurant, quoteMeal, requestMeal, Restaurant, Role, saveFood, saveRestaurant, seedDemo, Status, STORE_KEY, transitionOrder, verifyDemoOtp } from './offlineModel';

type Page='discover'|'orders'|'requests'|'menu'|'profile'|'applications'|'payments';
const titles:Record<Page,string>={discover:'Find your next favourite.',orders:'Every meal, one step closer.',requests:'A meal made for you.',menu:'Your menu. Your signature.',profile:'Make a great first impression.',applications:'Meet your next food partners.',payments:'A clear view of every order.'};
const Field=({label,children}:{label:string;children:React.ReactNode})=><label className="mh-field"><span>{label}</span>{children}</label>;
const Badge=({children}:{children:React.ReactNode})=><span className={`mh-badge ${String(children).toLowerCase().replaceAll(' ','-')}`}>{children}</span>;
const short=(value:string)=>new Date(value).toLocaleTimeString('en-GB',{hour:'2-digit',minute:'2-digit'});
const CSS=`
*{box-sizing:border-box}html,body,#root{margin:0;min-height:100%;background:#f8f7f2}button,input,select,textarea{font:inherit}button{cursor:pointer}button:disabled{opacity:.45;cursor:not-allowed}button:focus-visible,a:focus-visible,input:focus-visible,select:focus-visible,textarea:focus-visible{outline:3px solid #ed7d44;outline-offset:3px} .mh{font:15px/1.5 Arial,Helvetica,sans-serif;color:#20382d;background:#f8f7f2;min-height:100vh;letter-spacing:-.1px} .mh button{transition:background .15s,transform .15s} .mh button:active{transform:translateY(1px)} .mh h1,.mh h2,.mh h3,.mh p{margin:0} .mh small{font-size:12px}.mh-banner{background:#183e2c;color:#eef5df;padding:9px 28px;display:flex;justify-content:center;gap:14px;align-items:center;font-size:12px;flex-wrap:wrap}.mh-banner strong{letter-spacing:1.3px;font-size:10px;border:1px solid #6d896e;padding:2px 7px;border-radius:4px}.mh-banner button{color:white;border:0;background:none;text-decoration:underline}.mh-head{padding:20px 4.5%;display:flex;align-items:center;gap:24px;background:#fffefa;border-bottom:1px solid #e6e8dc}.mh-brand{font-size:28px;font-weight:900;letter-spacing:-1.3px;color:#183e2c;white-space:nowrap}.mh-brand b{color:#dc622b}.mh-mark{display:inline-grid;place-items:center;width:33px;height:33px;background:#df652f;color:white;border-radius:10px;margin-right:7px;font-size:21px;vertical-align:3px}.mh-location{font-size:12px;color:#647263;border-left:1px solid #ddd;padding-left:22px}.mh-location b{color:#20382d;display:block;font-size:14px}.mh-personas{display:flex;gap:3px;background:#f0f1e9;padding:4px;border-radius:12px;margin-left:auto}.mh-personas button{background:transparent;border:0;border-radius:9px;color:#637064;padding:9px 14px;font-size:12px;font-weight:bold}.mh-personas button.active{background:#fff;box-shadow:0 1px 4px #0001;color:#20382d}.mh-bell{border:1px solid #dde1d5;background:white;padding:10px 14px;border-radius:50px;white-space:nowrap}.mh-layout{display:grid;grid-template-columns:194px minmax(0,1fr);max-width:1540px;margin:auto}.mh-side{padding:30px 18px;border-right:1px solid #e6e8dc;display:flex;flex-direction:column;gap:6px}.mh-side small{letter-spacing:1.5px;color:#85917f;font-size:10px;margin:8px 12px 15px}.mh-side button{background:none;border:0;border-radius:10px;text-align:left;padding:12px 15px;color:#647063;font-size:13px;display:flex;gap:10px;align-items:center}.mh-side button.active{background:#e8eedf;color:#254730;font-weight:bold}.mh-side .mh-side-bottom{margin-top:50px;padding-top:18px;border-top:1px solid #e0e3d8}.mh-main{padding:34px 34px 60px;min-width:0}.mh-eyebrow{letter-spacing:2px;font-size:10px;color:#698063;font-weight:bold;text-transform:uppercase;margin-bottom:9px!important}.mh-pagehead{display:flex;justify-content:space-between;align-items:flex-end;gap:18px;margin-bottom:25px}.mh-pagehead h1{font:500 32px/1.1 Georgia,serif;letter-spacing:-1px}.mh-sub{color:#73816f;font-size:13px;margin-top:10px!important}.mh-hero{border-radius:22px;background:#e9eedc;padding:32px 35px;position:relative;overflow:hidden;display:grid;grid-template-columns:1.2fr 1fr;gap:14px;align-items:center;margin-bottom:27px}.mh-hero h1{font:500 clamp(37px,4vw,59px)/1.03 Georgia,serif;letter-spacing:-2px;color:#254a31;max-width:480px}.mh-hero h1 em{color:#d36736;font-style:italic}.mh-hero p{font-size:13px;color:#64735e;max-width:340px;margin:17px 0 23px}.mh-hero-art{position:relative;min-height:210px;display:grid;place-items:center}.mh-plate{width:195px;height:195px;border:14px solid #fffef4;border-radius:50%;background:#f8ddb5;box-shadow:15px 18px 0 #7a8c5120,0 0 0 1px #cdd3be;display:grid;place-items:center;font-size:102px;transform:rotate(-9deg)}.mh-float{position:absolute;border-radius:10px;padding:10px 13px;background:#fffdf1;box-shadow:0 6px 22px #38442912;font-size:11px}.mh-float.top{top:5px;right:0}.mh-float.bottom{bottom:0;left:0}.mh-primary{border:0;border-radius:9px;background:#d96732;color:#fff;padding:12px 19px;font-weight:bold;font-size:12px}.mh-secondary{border:1px solid #d8dfd0;border-radius:9px;background:#fffefa;color:#294631;padding:10px 15px;font-weight:bold;font-size:12px}.mh-danger{border:1px solid #e6c6bc;background:#fff4ef;color:#ae4429;border-radius:9px;padding:9px 12px;font-size:12px}.mh-toolbar{display:flex;gap:12px;margin-bottom:18px;align-items:center;flex-wrap:wrap}.mh-search{flex:1;min-width:190px;border:1px solid #dce1d3;border-radius:11px;background:white;padding:13px 17px;font-size:13px}.mh-toolbar select,.mh-pagehead select{border:1px solid #dce1d3;border-radius:9px;padding:11px;background:#fffefa;color:#36513a;max-width:100%}.mh-chips{display:flex;gap:8px;flex-wrap:wrap;margin-bottom:24px}.mh-chips button{border:1px solid #dde2d4;background:#fffefa;border-radius:30px;padding:8px 16px;color:#667361;font-size:12px}.mh-chips button.active{background:#284c34;color:white;border-color:#284c34}.mh-content{display:grid;grid-template-columns:minmax(0,1fr) 272px;gap:24px;align-items:start}.mh-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:18px}.mh-card{border:1px solid #e2e5da;border-radius:15px;background:#fffefa;overflow:hidden}.mh-foodart{background:#f3e7d3;height:151px;display:grid;place-items:center;position:relative}.mh-foodart.green{background:#e4ecd9}.mh-foodart.rose{background:#f2e0d7}.mh-foodart .dish{font-size:72px;border:9px solid #fff8e6;border-radius:100%;height:117px;width:117px;display:grid;place-items:center;background:#f3d4a2;box-shadow:7px 7px 0 #76593310}.mh-foodart img{width:100%;height:100%;object-fit:cover;position:absolute;inset:0}.mh-foodart .mh-badge{position:absolute;top:12px;left:12px;background:#fffefa;z-index:1}.mh-foodbody{padding:17px}.mh-foodbody small{font-size:10px;text-transform:uppercase;letter-spacing:.9px;color:#7c8973}.mh-foodbody h3{font-size:16px;margin:6px 0}.mh-foodbody p{font-size:12px;color:#7b8274;min-height:36px}.mh-row{display:flex;justify-content:space-between;align-items:center;gap:12px}.mh-foodbody .mh-row{margin-top:19px}.mh-foodbody strong{font-size:14px}.mh-add{border:1px solid #e4b49a;border-radius:8px;background:#fff5e9;padding:6px 13px;color:#bc562a;font-size:12px;font-weight:bold}.mh-bag{background:#fffefa;border:1px solid #e2e5da;border-radius:15px;padding:22px;position:sticky;top:20px}.mh-bag h2{font-size:19px}.mh-bag h2 span{color:#d36a35}.mh-empty{text-align:center;padding:42px 20px;color:#7b8772;border:1px dashed #d9dfcf;border-radius:12px;margin:18px 0}.mh-empty .symbol{font-size:32px;display:block;margin-bottom:10px}.mh-empty h3{color:#45583e;font-size:16px;margin-bottom:7px}.mh-empty p{font-size:12px}.mh-bag .mh-empty{border:0;padding:40px 0 25px}.mh-cartrow{border-bottom:1px solid #ebece4;padding:16px 0;font-size:12px}.mh-cartrow p{margin-bottom:8px}.mh-qty{display:flex;gap:10px;align-items:center}.mh-qty button{border:1px solid #e0e4d7;background:#f6f7ed;border-radius:5px;width:26px;height:26px}.mh-totals{padding:19px 0 12px;display:flex;justify-content:space-between;font-weight:bold}.mh-full{width:100%;margin-top:12px}.mh-fine{font-size:11px;color:#78856e;text-align:center;margin-top:12px!important}.mh-panel{padding:23px;background:#fffefa;border:1px solid #e2e5da;border-radius:14px;margin-bottom:16px}.mh-panel h3{font-size:16px}.mh-panel p{font-size:13px;color:#6b7b62;margin:10px 0}.mh-badge{padding:4px 9px;border-radius:6px;font-size:10px;background:#edf0e4;color:#567143;font-weight:bold;white-space:nowrap}.mh-badge.pending,.mh-badge.quoted{background:#fff0d7;color:#ac771e}.mh-badge.cancelled,.mh-badge.rejected{background:#fae8e2;color:#b45642}.mh-badge.completed,.mh-badge.approved{background:#e4eedf;color:#417044}.mh-actions{display:flex;gap:9px;flex-wrap:wrap;margin-top:17px}.mh-progress{display:flex;gap:4px;flex-wrap:wrap;margin:20px 0 8px}.mh-progress span{flex:1;min-width:65px;padding-top:8px;border-top:3px solid #edf0e5;color:#95a08c;font-size:10px}.mh-progress span.done{border-color:#749163;color:#385a31}.mh-metrics{display:grid;grid-template-columns:repeat(3,1fr);gap:15px;margin-bottom:25px}.mh-metric{padding:20px;background:#fffefa;border:1px solid #e2e5da;border-radius:13px}.mh-metric strong{display:block;font:28px Georgia,serif;margin-top:8px}.mh-metric span{font-size:11px;color:#7a8770}.mh-overlay{position:fixed;inset:0;background:#142a2280;z-index:100;display:flex;align-items:center;justify-content:center;padding:20px;backdrop-filter:blur(3px)}.mh-modal{background:#fffefa;padding:27px;border-radius:18px;width:560px;max-width:100%;max-height:90vh;overflow:auto;box-shadow:0 24px 80px #0002}.mh-modal h2{font:28px Georgia,serif}.mh-close{border:0;background:#f0f2e7;border-radius:50%;width:31px;height:31px;color:#4a6341;font-size:20px}.mh-field{display:flex;flex-direction:column;gap:7px;margin:15px 0;font-size:12px;color:#536749;font-weight:bold}.mh-field input,.mh-field textarea,.mh-field select{border:1px solid #d9dfcf;border-radius:8px;background:white;padding:11px;color:#294631;width:100%;font-size:14px;font-weight:normal}.mh-field textarea{min-height:85px;resize:vertical}.mh-two{display:grid;grid-template-columns:1fr 1fr;gap:0 15px}.mh-note{background:#f1f3e7;padding:13px;border-radius:9px;font-size:12px;color:#5c7050;margin:15px 0}.mh-toast{position:fixed;bottom:25px;left:50%;transform:translateX(-50%);padding:14px 24px;border-radius:10px;z-index:200;box-shadow:0 5px 25px #0002;background:#20482d;color:white;font-size:13px;max-width:90vw}.mh-toast.error{background:#9f422e}.mh-check{display:flex;gap:10px;align-items:center;font-size:13px;margin:15px 0}.mh-table{width:100%;border-collapse:collapse;font-size:12px}.mh-table th{text-align:left;color:#7e8b74;font-size:10px;text-transform:uppercase;letter-spacing:.7px}.mh-table td,.mh-table th{padding:14px 8px;border-bottom:1px solid #e6e9df}.mh-footer{font-size:11px;color:#8b967f;text-align:center;margin-top:35px}.mh-photo-preview{width:100%;height:170px;object-fit:cover;border-radius:10px}.mh-small-title{font:23px Georgia,serif;margin-bottom:18px!important}
@media(min-width:1450px){.mh-grid{grid-template-columns:repeat(3,minmax(0,1fr))}}@media(max-width:1100px){.mh-layout{grid-template-columns:155px 1fr}.mh-main{padding:26px 22px}.mh-content{grid-template-columns:1fr}.mh-bag{position:static}.mh-hero{padding:28px}.mh-head{gap:15px}.mh-location{display:none}}@media(max-width:700px){.mh-head{padding:15px 16px;flex-wrap:wrap;gap:13px}.mh-brand{font-size:25px}.mh-personas{order:3;width:100%;margin:0}.mh-personas button{flex:1}.mh-bell{margin-left:auto;font-size:11px}.mh-layout{display:block}.mh-side{flex-direction:row;overflow:auto;padding:10px;border-right:0;border-bottom:1px solid #e2e5d9;gap:0}.mh-side button{white-space:nowrap;font-size:11px;padding:10px}.mh-side small,.mh-side .mh-side-bottom{display:none}.mh-main{padding:23px 16px 45px}.mh-hero{grid-template-columns:1fr;padding:26px}.mh-hero-art{display:none}.mh-hero h1{font-size:44px}.mh-grid{gap:12px}.mh-foodbody{padding:13px}.mh-foodbody h3{font-size:14px}.mh-foodbody small{font-size:8px}.mh-foodbody p{font-size:11px}.mh-foodbody strong{font-size:12px}.mh-add{padding:6px 10px}.mh-pagehead{align-items:flex-start;flex-direction:column}.mh-pagehead h1{font-size:30px}.mh-foodart{height:126px}.mh-foodart .dish{height:95px;width:95px;font-size:55px}.mh-metrics{gap:8px}.mh-metric{padding:12px}.mh-metric strong{font-size:22px}.mh-panel{padding:18px}.mh-banner{padding:8px 12px;font-size:10px;gap:7px}.mh-two{grid-template-columns:1fr}.mh-modal{padding:22px}.mh-table{min-width:470px}}
`;

export default function OfflineDemoWeb(){
 const [state,setState]=useState<DemoState>(seedDemo),[ready,setReady]=useState(false),[role,setRole]=useState<Role>('customer'),[page,setPage]=useState<Page>('discover');
 const [restaurantId,setRestaurantId]=useState('amina'),[search,setSearch]=useState(''),[category,setCategory]=useState('All'),[filterRestaurant,setFilterRestaurant]=useState('all'),[cart,setCart]=useState<Record<string,number>>({});
 const [modal,setModal]=useState<string|null>(null),[editing,setEditing]=useState<Food|null>(null),[toast,setToast]=useState<{text:string;error:boolean}|null>(null),[otp,setOtp]=useState<DemoOtp|null>(null),[otpInput,setOtpInput]=useState('');
 useEffect(()=>{try{const raw=localStorage.getItem(STORE_KEY);if(raw){const parsed=JSON.parse(raw);if(parsed.version!==1||!['restaurants','foods','orders','applications','requests','notices'].every(k=>Array.isArray(parsed[k])))throw new Error('Incompatible saved demo.');setState(parsed);}}catch{setToast({text:'Saved demo data could not be loaded. A fresh sample is shown. Reset demo to save a clean copy.',error:true});}setReady(true);},[]);
 useEffect(()=>{if(!toast)return;const timer=setTimeout(()=>setToast(null),7000);return()=>clearTimeout(timer);},[toast]);
 useEffect(()=>{if(!modal)return;const close=(e:KeyboardEvent)=>{if(e.key==='Escape')setModal(null);};document.addEventListener('keydown',close);return()=>document.removeEventListener('keydown',close);},[modal]);
 const tell=(text:string,error=false)=>setToast({text,error});
 const commit=(action:(s:DemoState)=>void,message?:string)=>{try{const updated=mutate(state,action);localStorage.setItem(STORE_KEY,JSON.stringify(updated));setState(updated);if(message)tell(message);return true;}catch(e){tell(e instanceof Error?e.message:'Unable to save this change.',true);return false;}};
 const restaurant=state.restaurants.find(r=>r.id===restaurantId)||state.restaurants[0];
 const nav: {page:Page;label:string;icon:string}[]=role==='customer'?[{page:'discover',label:'Discover food',icon:'⌕'},{page:'orders',label:'My orders',icon:'▤'},{page:'requests',label:'Custom meals',icon:'♧'}]:role==='restaurant'?[{page:'orders',label:'Kitchen orders',icon:'▤'},{page:'menu',label:'Menu & photos',icon:'◉'},{page:'requests',label:'Meal requests',icon:'♧'},{page:'profile',label:'Restaurant profile',icon:'⌂'},{page:'payments',label:'Payments',icon:'◈'}]:[{page:'applications',label:'Applications',icon:'▧'},{page:'orders',label:'All orders',icon:'▤'},{page:'payments',label:'Payment overview',icon:'◈'}];
 const switchRole=(value:Role)=>{setRole(value);setPage(value==='customer'?'discover':value==='restaurant'?'orders':'applications');setModal(null);};
 const visibleOrders=state.orders.filter(o=>role!=='restaurant'||o.restaurantId===restaurant.id);
 const visibleRequests=state.requests.filter(r=>role!=='restaurant'||r.restaurantId===restaurant.id);
 const cartItems=Object.entries(cart).filter(([,n])=>n>0).map(([id,quantity])=>({food:state.foods.find(f=>f.id===id)!,quantity})).filter(x=>x.food);
 const total=cartItems.reduce((n,x)=>n+x.food.price*x.quantity,0);
 const add=(food:Food)=>{if(cartItems.length&&cartItems[0].food.restaurantId!==food.restaurantId){tell('Complete or clear your current bag before ordering from another restaurant.',true);return;}if((cart[food.id]||0)>=50){tell('Maximum 50 portions per item.',true);return;}setCart({...cart,[food.id]:(cart[food.id]||0)+1});};
 const filtered=state.foods.filter(f=>{const r=state.restaurants.find(x=>x.id===f.restaurantId);return r?.published&&(filterRestaurant==='all'||r.id===filterRestaurant)&&(category==='All'||f.category===category)&&`${f.name} ${f.description} ${r.name} ${r.area}`.toLowerCase().includes(search.toLowerCase());});
 const unread=state.notices.filter(n=>n.role===role&&!n.read).length;
 const openFood=(food?:Food)=>{setEditing(food||{id:'',restaurantId:restaurant.id,name:'',description:'',category:'Signature meals',price:0,available:true,photo:'',emoji:'🍽️'});setModal('food');};
 const close=()=>setModal(null);
 const submit=(event:React.FormEvent<HTMLFormElement>,action:(data:FormData)=>void)=>{event.preventDefault();action(new FormData(event.currentTarget));};
 const text=(d:FormData,key:string)=>String(d.get(key)||'').trim();
 if(!ready)return <div className="mh"><style>{CSS}</style><main className="mh-main"><h1>Preparing your MloHub demo…</h1></main></div>;
 return <div className="mh"><style>{CSS}</style>
  <div className="mh-banner"><strong>OFFLINE DEMO</strong><span>Sample data · SMS, payments & syncing are simulated · No money moves</span><button onClick={()=>setModal('about')}>Demo guide ↗</button></div>
  <header className="mh-head"><div className="mh-brand"><span className="mh-mark">m</span>mlo<b>hub</b><span style={{fontSize:11,letterSpacing:0,marginLeft:7,color:'#83906f'}}> / demo</span></div><div className="mh-location">YOUR NEIGHBOURHOOD<b>⌖ Dar es Salaam</b></div><div className="mh-personas" aria-label="Demo personas">{(['customer','restaurant','admin'] as Role[]).map(r=><button key={r} className={role===r?'active':''} aria-pressed={role===r} onClick={()=>switchRole(r)}>{r==='customer'?'Customer':r==='restaurant'?'Restaurant':'Admin'}</button>)}</div><button className="mh-bell" aria-label={`Notifications, ${unread} unread`} onClick={()=>setModal('notifications')}>♧ Notifications {unread>0&&<b>· {unread}</b>}</button></header>
  <div className="mh-layout"><aside className="mh-side"><small>{role==='customer'?'YOUR DAILY GOOD FOOD':role==='restaurant'?'YOUR KITCHEN':'YOUR COMMUNITY'}</small>{nav.map(n=><button key={n.page} className={page===n.page?'active':''} onClick={()=>setPage(n.page)}><span>{n.icon}</span>{n.label}</button>)}<div className="mh-side-bottom"><button onClick={()=>setModal('register')}>＋ Register restaurant</button><button onClick={()=>setModal('otp')}>◌ Try demo OTP</button><button onClick={()=>setModal('reset')}>↻ Reset sample data</button></div></aside>
  <main className="mh-main">
   {page==='discover'?<section className="mh-hero"><div><div className="mh-eyebrow">Made nearby. Made with care.</div><h1>Good food.<br/><em>Closer to you.</em></h1><p>Discover local kitchens, find your favourite meal, and make every day a little more delicious.</p><button className="mh-primary" onClick={()=>document.getElementById('mh-search')?.focus()}>Find something delicious ↗</button></div><div className="mh-hero-art" aria-hidden="true"><div className="mh-plate">🍛</div><div className="mh-float top">✦ Made fresh in your neighbourhood</div><div className="mh-float bottom"><b>A little taste of home.</b><br/>Swahili favourites, made with love</div></div></section>:<div className="mh-pagehead"><div><div className="mh-eyebrow">{role==='customer'?'Your MloHub':role==='restaurant'?'Restaurant workspace':'Admin workspace'}</div><h1>{titles[page]}</h1><p className="mh-sub">{role==='restaurant'?restaurant.name:'All activity here stays in this browser.'}</p></div>{role==='restaurant'&&<select aria-label="Active restaurant" value={restaurant.id} onChange={e=>setRestaurantId(e.target.value)}>{state.restaurants.map(r=><option key={r.id} value={r.id}>{r.name}</option>)}</select>}</div>}
   {page==='discover'&&<><div className="mh-toolbar"><input id="mh-search" className="mh-search" aria-label="Search food or area" placeholder="⌕  What are you craving today?" value={search} onChange={e=>setSearch(e.target.value)}/><select aria-label="Filter restaurant" value={filterRestaurant} onChange={e=>setFilterRestaurant(e.target.value)}><option value="all">All neighbourhood kitchens</option>{state.restaurants.filter(r=>r.published).map(r=><option key={r.id} value={r.id}>{r.name} · {r.area}</option>)}</select></div><div className="mh-chips">{['All',...new Set(state.foods.map(f=>f.category))].map(c=><button key={c} className={category===c?'active':''} onClick={()=>setCategory(c)}>{c}</button>)}</div><div className="mh-content"><div><div className="mh-row" style={{marginBottom:17}}><h2 className="mh-small-title" style={{marginBottom:0}}>A good meal starts here</h2><small style={{color:'#8b967f'}}>{filtered.length} dishes</small></div>{!filtered.length?<Empty title="No meals match just yet" body="Try another search, kitchen, or category."/>:<div className="mh-grid">{filtered.map((f,index)=>{const r=state.restaurants.find(r=>r.id===f.restaurantId)!;return <article className="mh-card" key={f.id}><div className={`mh-foodart ${index%3===1?'green':index%3===2?'rose':''}`}>{f.photo?<img src={f.photo} alt={f.name}/>:<span className="dish" aria-hidden="true">{f.emoji}</span>}<Badge>{!r.open?'Kitchen closed':!f.available?'Sold out':r.area}</Badge></div><div className="mh-foodbody"><small>{r.name}</small><h3>{f.name}</h3><p>{f.description}</p><div className="mh-row"><strong>{money(f.price)}</strong><button aria-label={`Add ${f.name}`} className="mh-add" disabled={!f.available||!r.open} onClick={()=>add(f)}>＋ Add</button></div></div></article>;})}</div>}</div><aside className="mh-bag"><div className="mh-row"><h2>Your bag <span>· {cartItems.reduce((n,x)=>n+x.quantity,0)}</span></h2>{cartItems.length>0&&<button className="mh-add" onClick={()=>setCart({})}>Clear</button>}</div>{!cartItems.length?<Empty title="Something delicious awaits" body="Add a meal to start your order." symbol="🛍️"/>:<>{cartItems.map(({food,quantity})=><div className="mh-cartrow" key={food.id}><p><b>{food.name}</b></p><div className="mh-row"><div className="mh-qty"><button aria-label={`Remove one ${food.name}`} onClick={()=>setCart({...cart,[food.id]:Math.max(0,quantity-1)})}>−</button><span>{quantity}</span><button aria-label={`Add one ${food.name}`} onClick={()=>add(food)}>＋</button></div><span>{money(food.price*quantity)}</span></div></div>)}<div className="mh-totals"><span>Total</span><span>{money(total)}</span></div><small>Pickup · no delivery fee</small><button className="mh-primary mh-full" onClick={()=>setModal('checkout')}>Review order →</button></>}<p className="mh-fine">Prepared with care.<br/>Demo orders stay on this device.</p></aside></div><div className="mh-panel" style={{marginTop:25}}><div className="mh-row"><div><h3>Cooking something bigger?</h3><p>Tell a kitchen what you need and receive a custom meal quote.</p></div><button className="mh-secondary" onClick={()=>{setPage('requests');setModal('request');}}>Request a meal ↗</button></div></div></>}
   {page==='orders'&&<><div className="mh-metrics"><Metric label="Waiting for a kitchen" value={visibleOrders.filter(o=>o.status==='Pending').length}/><Metric label="In progress" value={visibleOrders.filter(o=>['Accepted','Preparing','Ready'].includes(o.status)).length}/><Metric label="Completed meals" value={visibleOrders.filter(o=>o.status==='Completed').length}/></div>{!visibleOrders.length?<Empty title="Your first order starts a story" body={role==='customer'?'Choose a meal from Discover food to see its journey here.':'Place a sample order as Customer, then return here to accept it.'}/>:visibleOrders.map(o=><section className="mh-panel" key={o.id}><div className="mh-row"><div><h3>{state.restaurants.find(r=>r.id===o.restaurantId)?.name}</h3><small>{o.id} · {short(o.createdAt)}</small></div><Badge>{o.status}</Badge></div><p>{o.items.map(i=>`${i.quantity} × ${i.name}`).join(' · ')}</p><div className="mh-row"><b>{money(o.total)}</b><small>{o.paymentStatus}</small></div>{o.status!=='Cancelled'&&<div className="mh-progress">{(['Pending','Accepted','Preparing','Ready','Completed'] as Status[]).map(s=><span key={s} className={o.history.some(h=>h.status===s)?'done':''}>{s}</span>)}</div>}<div className="mh-actions">{role!=='customer'&&o.status==='Pending'&&<button className="mh-primary" onClick={()=>commit(s=>transitionOrder(s,o.id,'Accepted',role),'Order accepted. The customer timeline has updated.')}>Accept order</button>}{role!=='customer'&&['Accepted','Preparing','Ready'].includes(o.status)&&<button className="mh-primary" onClick={()=>commit(s=>transitionOrder(s,o.id,({Accepted:'Preparing',Preparing:'Ready',Ready:'Completed'} as Record<string,Status>)[o.status],role),'Order status updated.')}>{o.status==='Accepted'?'Start preparing':o.status==='Preparing'?'Mark ready':'Complete pickup'}</button>}{(o.status==='Pending'||role!=='customer'&&o.status==='Accepted')&&<button className="mh-danger" onClick={()=>commit(s=>transitionOrder(s,o.id,'Cancelled',role),'Order cancelled. Any demo payment is marked simulated refunded.')}>Cancel order</button>}</div></section>)}</>}
   {page==='requests'&&<>{role==='customer'&&<button className="mh-primary" style={{marginBottom:20}} onClick={()=>setModal('request')}>＋ New custom meal request</button>}{!visibleRequests.length?<Empty title="Made for your table" body="Create a group meal request, switch to Restaurant to quote, then accept it as Customer."/>:visibleRequests.map(r=><div className="mh-panel" key={r.id}><div className="mh-row"><h3>{r.description}</h3><Badge>{r.status}</Badge></div><p>{r.people} people · Budget {money(r.budget)} total · {state.restaurants.find(x=>x.id===r.restaurantId)?.name}</p>{r.quote&&<b>Kitchen quote: {money(r.quote)} total</b>}{r.orderId&&<p>Order {r.orderId} is now in My orders.</p>}<div className="mh-actions">{role==='restaurant'&&r.status==='Pending'&&<form className="mh-toolbar" onSubmit={e=>submit(e,d=>commit(s=>quoteMeal(s,r.id,Number(d.get('quote'))),'Quote sent to the demo customer.'))}><input className="mh-search" aria-label="Total quote TZS" name="quote" type="number" min="1" step="1" required placeholder="Total quote in TZS"/><button className="mh-primary">Send quote</button></form>}{role==='customer'&&r.status==='Quoted'&&<button className="mh-primary" onClick={()=>commit(s=>acceptQuote(s,r.id),'Quote accepted. Your new order is waiting for the kitchen.')}>Accept quote</button>}{role==='customer'&&['Pending','Quoted'].includes(r.status)&&<button className="mh-danger" onClick={()=>commit(s=>cancelRequest(s,r.id),'Request cancelled.')}>Cancel request</button>}</div></div>)}</>}
   {page==='menu'&&<><div className="mh-toolbar"><button className="mh-primary" onClick={()=>openFood()}>＋ Add a dish</button><span className="mh-sub">Edits appear immediately in Customer discovery.</span></div><div className="mh-grid">{state.foods.filter(f=>f.restaurantId===restaurant.id).map(f=><div className="mh-panel" key={f.id}><div className="mh-row"><span style={{fontSize:30}}>{f.emoji}</span><Badge>{f.available?'Available':'Sold out'}</Badge></div>{f.photo&&<img className="mh-photo-preview" src={f.photo} alt={f.name}/>}<h3 style={{marginTop:12}}>{f.name}</h3><p>{f.description}</p><b>{money(f.price)}</b><div className="mh-actions"><button className="mh-secondary" onClick={()=>openFood(f)}>Edit dish</button><button className="mh-secondary" onClick={()=>commit(s=>saveFood(s,{...f,available:!f.available}),'Availability updated.')}>{f.available?'Mark sold out':'Make available'}</button></div></div>)}</div>{!state.foods.some(f=>f.restaurantId===restaurant.id)&&<Empty title="Your menu begins here" body="Add your first dish, then publish from Restaurant profile."/>}</>}
   {page==='profile'&&<div className="mh-panel"><div className="mh-row"><h3>Kitchen details</h3><Badge>{restaurant.published?'Published':'Draft'}</Badge></div><form key={restaurant.id} onSubmit={e=>submit(e,d=>{commit(s=>saveRestaurant(s,{...restaurant,name:text(d,'name'),description:text(d,'description'),area:text(d,'area'),address:text(d,'address'),phone:text(d,'phone'),cuisine:text(d,'cuisine'),hours:text(d,'hours'),lat:Number(d.get('lat')),lng:Number(d.get('lng')),open:d.has('open')}),'Restaurant profile saved. Customer discovery has updated.');})}><div className="mh-two">{(['name','cuisine','area','phone','address','hours'] as const).map(key=><Field key={key} label={key[0].toUpperCase()+key.slice(1)}><input name={key} defaultValue={restaurant[key]} required maxLength={150}/></Field>)}</div><Field label="About your kitchen"><textarea name="description" defaultValue={restaurant.description} maxLength={400}/></Field><div className="mh-two"><Field label="Latitude"><input name="lat" type="number" step="any" min="-90" max="90" required defaultValue={restaurant.lat}/></Field><Field label="Longitude"><input name="lng" type="number" step="any" min="-180" max="180" required defaultValue={restaurant.lng}/></Field></div><label className="mh-check"><input type="checkbox" name="open" defaultChecked={restaurant.open}/>Open for orders</label><p className="mh-note">Location is stored locally for this demonstration. No map service or GPS request is made.</p><div className="mh-actions"><button className="mh-primary">Save profile</button>{!restaurant.published&&<button type="button" className="mh-secondary" onClick={()=>commit(s=>publishRestaurant(s,restaurant.id),'Restaurant published in demo discovery.')}>Publish restaurant</button>}</div></form></div>}
   {page==='applications'&&<><div className="mh-metrics"><Metric label="Waiting for review" value={state.applications.filter(a=>a.status==='Pending').length}/><Metric label="Demo kitchens" value={state.restaurants.length}/><Metric label="Published kitchens" value={state.restaurants.filter(r=>r.published).length}/></div><button className="mh-primary" style={{marginBottom:20}} onClick={()=>setModal('register')}>＋ Submit sample application</button>{!state.applications.length?<Empty title="A growing food community" body="Register a sample restaurant to demonstrate approval, menu setup, and publishing."/>:state.applications.map(a=><div className="mh-panel" key={a.id}><div className="mh-row"><h3>{a.name}</h3><Badge>{a.status}</Badge></div><p>{a.owner} · {a.cuisine} · {a.area}<br/>{a.address}<br/>{a.phone}</p>{a.status==='Pending'&&<div className="mh-actions"><button className="mh-primary" onClick={()=>commit(s=>decideApplication(s,a.id,true),'Approved. Switch to Restaurant and choose the new kitchen to add its menu.')}>Approve application</button><button className="mh-danger" onClick={()=>commit(s=>decideApplication(s,a.id,false),'Application rejected.')}>Reject application</button></div>}</div>)}</>}
   {page==='payments'&&<><div className="mh-note">No real charges, transfers, settlements, or refunds are made. These entries describe local sample orders only.</div><div className="mh-metrics"><Metric label="Simulated payments" value={money(visibleOrders.filter(o=>o.paymentStatus==='Simulated paid').reduce((n,o)=>n+o.total,0))}/><Metric label="Simulated refunds" value={money(visibleOrders.filter(o=>o.paymentStatus==='Simulated refunded').reduce((n,o)=>n+o.total,0))}/><Metric label="Orders" value={visibleOrders.length}/></div><div className="mh-panel" style={{overflowX:'auto'}}><table className="mh-table"><thead><tr><th>Order</th><th>Amount</th><th>Method</th><th>Payment status</th></tr></thead><tbody>{visibleOrders.map(o=><tr key={o.id}><td>{o.id}</td><td>{money(o.total)}</td><td>{o.payment}</td><td>{o.paymentStatus}</td></tr>)}</tbody></table>{!visibleOrders.length&&<Empty title="No demo payment activity" body="Place a sample order with simulated mobile money to see it here."/>}</div></>}
   <footer className="mh-footer">Made for a little more delicious. · MloHub offline demonstration · Sample businesses and contacts</footer>
  </main></div>
  {modal&&<div className="mh-overlay" onClick={e=>{if(e.target===e.currentTarget)close();}}><section className="mh-modal" role="dialog" aria-modal="true" aria-labelledby="mh-modal-title"><div className="mh-row"><h2 id="mh-modal-title">{{checkout:'Your next delicious moment.',food:editing?.id?'Edit your dish':'Add something delicious',register:'Join the neighbourhood.',request:'Made for your table.',notifications:'Your latest updates.',otp:'Try demo verification.',reset:'A fresh start?',about:'Your demonstration guide.'}[modal]}</h2><button className="mh-close" aria-label="Close dialog" onClick={close}>×</button></div>
   {modal==='checkout'&&<form onSubmit={e=>submit(e,d=>{if(commit(s=>placeOrder(s,cartItems[0]?.food.restaurantId,cart,text(d,'payment') as Order['payment']),'Demo order placed. Switch to Restaurant to accept it.')){setCart({});setPage('orders');close();}})}><p className="mh-sub">Pickup from {state.restaurants.find(r=>r.id===cartItems[0]?.food.restaurantId)?.name}</p>{cartItems.map(x=><div className="mh-cartrow mh-row" key={x.food.id}><span>{x.quantity} × {x.food.name}</span><b>{money(x.food.price*x.quantity)}</b></div>)}<div className="mh-totals"><span>Total</span><span>{money(total)}</span></div><Field label="Demo payment method"><select name="payment"><option>Cash on pickup</option><option>Simulated mobile money</option></select></Field><div className="mh-note">No real payment or SMS. Mobile money is recorded as a simulated payment immediately. No PIN or real account details are requested.</div><button className="mh-primary mh-full">Place demo order →</button></form>}
   {modal==='food'&&editing&&<form onSubmit={e=>submit(e,d=>{if(commit(s=>saveFood(s,{...editing,name:text(d,'name'),description:text(d,'description'),category:text(d,'category'),price:Number(d.get('price')),available:d.has('available')}),'Menu saved. Your customer view is up to date.'))close();})}><Field label="Dish name"><input name="name" defaultValue={editing.name} required maxLength={100}/></Field><Field label="Description"><textarea name="description" defaultValue={editing.description} maxLength={300}/></Field><div className="mh-two"><Field label="Price in TZS"><input name="price" type="number" min="1" step="1" defaultValue={editing.price||''} required/></Field><Field label="Category"><input name="category" defaultValue={editing.category} required maxLength={40}/></Field></div><Field label="Food photo · JPG, PNG or WebP · up to 1 MB"><input type="file" accept="image/png,image/jpeg,image/webp" onChange={e=>{const file=e.target.files?.[0];if(!file)return;if(!['image/png','image/jpeg','image/webp'].includes(file.type)||file.size>1024*1024){tell('Use a JPG, PNG, or WebP image smaller than 1 MB.',true);return;}const reader=new FileReader();reader.onerror=()=>tell('Could not read this photo.',true);reader.onload=()=>setEditing(current=>current?{...current,photo:String(reader.result)}:current);reader.readAsDataURL(file);}}/></Field>{editing.photo&&<><img className="mh-photo-preview" src={editing.photo} alt="Selected food photo"/><button type="button" className="mh-danger" onClick={()=>setEditing({...editing,photo:''})}>Remove photo</button></>}<label className="mh-check"><input type="checkbox" name="available" defaultChecked={editing.available}/>Available to order</label><button className="mh-primary mh-full">Save dish</button></form>}
   {modal==='register'&&<form onSubmit={e=>submit(e,d=>{if(commit(s=>applyRestaurant(s,{name:text(d,'name'),owner:text(d,'owner'),phone:text(d,'phone'),area:text(d,'area'),cuisine:text(d,'cuisine'),address:text(d,'address')}),'Application submitted. Review it in Admin → Applications.'))close();})}><p className="mh-note">Use invented demonstration details. Approval creates a draft kitchen; add a dish and publish from the Restaurant workspace.</p>{[['name','Restaurant name'],['owner','Owner name'],['phone','Sample contact phone'],['area','Neighbourhood'],['cuisine','Cuisine'],['address','Sample street address']].map(([name,label])=><Field key={name} label={label}><input name={name} required maxLength={120}/></Field>)}<button className="mh-primary mh-full">Submit demo application</button></form>}
   {modal==='request'&&<form onSubmit={e=>submit(e,d=>{if(commit(s=>requestMeal(s,{restaurantId:text(d,'restaurant'),description:text(d,'description'),people:Number(d.get('people')),budget:Number(d.get('budget'))}),'Request sent. Switch to Restaurant to offer a quote.')){setPage('requests');close();}})}><Field label="Choose a kitchen"><select name="restaurant">{state.restaurants.filter(r=>r.published&&r.open).map(r=><option key={r.id} value={r.id}>{r.name}</option>)}</select></Field><Field label="What would you like?"><textarea name="description" placeholder="For example: Swahili lunch for our team, with vegetarian options" required maxLength={300}/></Field><div className="mh-two"><Field label="Number of people"><input name="people" type="number" min="1" max="100" step="1" required/></Field><Field label="Total budget in TZS"><input name="budget" type="number" min="1" step="1" required/></Field></div><button className="mh-primary mh-full">Send meal request</button></form>}
   {modal==='notifications'&&<><div className="mh-actions"><button className="mh-secondary" onClick={()=>commit(s=>s.notices.forEach(n=>{if(n.role===role)n.read=true;}),'Notifications marked read.')}>Mark all read</button></div>{state.notices.filter(n=>n.role===role).map(n=><div className="mh-cartrow" key={n.id}><p><b>{!n.read?'● ':''}{n.text}</b></p><small>{short(n.at)} · Local demo update</small></div>)}{!state.notices.some(n=>n.role===role)&&<Empty title="You’re all caught up" body="Order and restaurant activity will appear here."/>}</>}
   {modal==='otp'&&<><p className="mh-note">This demonstrates verification states without sending an SMS or signing in to a real account. The sample code is intentionally visible.</p><button className="mh-secondary" onClick={()=>{setOtp(createDemoOtp());setOtpInput('');}}>Generate demo code</button>{otp&&<><p className="mh-note">Demo code: <b style={{fontSize:24,letterSpacing:5}}>{otp.code}</b><br/>Expires after five minutes. Maximum five attempts.</p><form onSubmit={e=>{e.preventDefault();try{const updated=verifyDemoOtp(otp,otpInput);setOtp(updated);tell(updated.verified?'Demo phone verification complete.':`Incorrect code. ${5-updated.attempts} attempts remain.`,!updated.verified);}catch(error){tell((error as Error).message,true);}}}><Field label="Enter the demo code"><input value={otpInput} onChange={e=>setOtpInput(e.target.value)} inputMode="numeric" pattern="[0-9]{6}" maxLength={6} required/></Field><button className="mh-primary" disabled={otp.verified||otp.attempts>=5}>{otp.verified?'Verified ✓':'Verify demo code'}</button></form></>}</>}
   {modal==='reset'&&<><p className="mh-note">This clears only this offline demonstration’s sample orders, photos, applications, and notifications. Your real MloHub files and Supabase data are unaffected.</p><button className="mh-danger" onClick={()=>{try{const fresh=seedDemo();localStorage.setItem(STORE_KEY,JSON.stringify(fresh));setState(fresh);setCart({});setRestaurantId('amina');setFilterRestaurant('all');setCategory('All');setSearch('');setOtp(null);setRole('customer');setPage('discover');close();tell('Sample data reset. Ready for a fresh demonstration.');}catch{tell('Your browser could not save the reset.',true);}}}>Reset this demo</button></>}
   {modal==='about'&&<><p className="mh-note">Three demo personas share one local dataset in this browser. Switching roles simulates customer-to-kitchen updates. It is not Supabase realtime, secure authentication, or a multi-device service.</p><ol style={{paddingLeft:20,fontSize:14,lineHeight:1.9}}><li><b>Customer:</b> discover food, add to bag, and place a demo order.</li><li><b>Restaurant:</b> accept, prepare, mark ready, and complete pickup.</li><li><b>Customer:</b> review the updated order and notifications.</li><li>Try a custom meal request and quote, or register a restaurant for admin approval.</li><li>Edit a dish, photo, price, or profile and return to discovery.</li></ol><div className="mh-actions"><button className="mh-secondary" onClick={()=>setModal('register')}>Register restaurant</button><button className="mh-secondary" onClick={()=>setModal('otp')}>Try OTP</button><button className="mh-danger" onClick={()=>setModal('reset')}>Reset demo</button></div><p className="mh-fine">No internet, external images, API credentials, real customer data, or real payments are needed. Demo data persists in this browser until reset.</p></>}
  </section></div>}
  {toast&&<div className={`mh-toast ${toast.error?'error':''}`} role="status" aria-live="polite">{toast.text}</div>}
 </div>;
}
function Empty({title,body,symbol='◌'}:{title:string;body:string;symbol?:string}){return <div className="mh-empty"><span className="symbol" aria-hidden="true">{symbol}</span><h3>{title}</h3><p>{body}</p></div>;}
function Metric({label,value}:{label:string;value:string|number}){return <div className="mh-metric"><span>{label}</span><strong>{value}</strong></div>;}
